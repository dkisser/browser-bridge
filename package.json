{
  "name": "browser-bridge",
  "private": true,
  "version": "0.3.1",
  "description": "A pure TypeScript + Bun monorepo bridging browser extensions and WebSocket services.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:core": "bun --cwd apps/bridge-core dev",
    "dev:extension": "bun --cwd apps/extension dev",
    "build:extension": "bun --cwd apps/extension build",
    "build:cli": "bun build --compile apps/cli/src/index.ts --outfile dist/bridge",
    "build:binaries": "bash .github/scripts/build-binaries.sh",
    "build:tarball": "bash .github/scripts/build-tarball.sh",
    "build:installer": "bash .github/scripts/build-installer.sh",
    "cli": "bun --cwd apps/cli start",
    "type-check": "tsc --noEmit",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:install": "bats install/tests/",
    "release:extension-zip": "bash .github/scripts/build-extension-zip.sh"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "@types/bun": "latest",
    "typescript": "^5.4.0"
  }
}
