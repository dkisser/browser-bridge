name: base-check

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Type check
        run: bun run type-check

      - name: Run unit/integration tests
        run: bun run test

      - name: Verify binaries compile
        run: |
          mkdir -p dist
          bun build --compile apps/websocket/src/index.ts --outfile dist/ws-server --target=bun-linux-x64
          bun build --compile apps/local-proxy/src/index.ts --outfile dist/local-proxy --target=bun-linux-x64
          bun build --compile apps/cli/src/index.ts --outfile dist/bridge-cmd --target=bun-linux-x64
          test -x dist/ws-server
          test -x dist/local-proxy
          test -x dist/bridge-cmd
